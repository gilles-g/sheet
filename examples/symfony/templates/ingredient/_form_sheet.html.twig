<div class="sheet-wrapper">
    <div class="sheet-header">
        <h2>{{ ingredient is defined ? 'Edit Ingredient' : 'Create Ingredient' }}</h2>
        <button data-action="click->sheet#close" class="btn-close" aria-label="Close"></button>
    </div>
    
    <div class="sheet-scrollpane">
        <div class="sheet-content">
            {{ form_start(form, {
                'attr': {
                    'data-turbo': 'true',
                    'data-turbo-frame': 'ingredient-form-frame'
                }
            }) }}
            
            <turbo-frame id="ingredient-form-frame">
                {% if form.vars.errors|length > 0 %}
                    <div class="alert alert-danger">
                        {{ form_errors(form) }}
                    </div>
                {% endif %}
                
                <div class="mb-3">
                    {{ form_row(form.name) }}
                </div>
                
                <div class="mb-3">
                    {{ form_row(form.description) }}
                </div>
                
                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-primary">
                        {{ ingredient is defined ? 'Update Ingredient' : 'Create Ingredient' }}
                    </button>
                    <button type="button" 
                            data-action="click->sheet#close" 
                            class="btn btn-secondary">
                        Cancel
                    </button>
                </div>
            </turbo-frame>
            
            {{ form_end(form) }}
        </div>
    </div>
</div>
